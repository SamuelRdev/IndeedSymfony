{% extends 'base.html.twig' %}

{% block body %}

    {% if editMode %}
        <h1 class='text-center'>Modifier un contrat</h1>
    {% else %}
        <h1 class='text-center'>Créer un nouveau contrat</h1>
    {% endif %}

    <div class="container">
        <div class="row">
            <div class="col-md-6 offset-md-3">
                <div class="form-control pe-5 ps-5 mb-5">
                    {{ form(form) }}
                </div>
            </div>
        </div>
    </div>
    
{% endblock %}