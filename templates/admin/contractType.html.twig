{% extends 'base.html.twig' %}

{% block body %}

    {% if editMode %}
        <h1 class ='text-center'>Créer un type de contrat</h1>
    {% else %}
        <h1 class='text-center'>Modifier un type de contrat</h1>
    {% endif %}

    <div class="container">
        <div class="row">
            <div class="col-md-6 offset-md-3">
                <div class="form-control pe-5 ps-5 mb-5">
                    {{ form_start(form) }}
                        {{ form_row(form.name) }}
                            <div class="button text-center">
                                <button type="submit" class='btn btn-success'>
                                    {% if editMode %}
                                        Enregistrer
                                    {% else %}
                                        Ajouter
                                    {% endif %}
                                </button>
                            </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
   
{% endblock %}